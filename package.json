{
  "name": "ghql",
  "version": "0.0.0",
  "main": "index.js",
  "license": "AGPL-3.0",
  "scripts": {
    "deps": "standard && dependency-check . && dependency-check . --unused --no-dev",
    "lint": "standard"
  },
  "devDependencies": {
    "dependency-check": "^3.0.0",
    "standard": "^10.0.3"
  },
  "dependencies": {
    "JSONStream": "^1.3.2",
    "dotenv": "^5.0.0"
  }
}
